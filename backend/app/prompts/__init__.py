"""AI prompt templates — system prompts, user-prompt builders, and JSON schemas.

Kept separate from services so the marketing craft lives in one place and the
schemas double as the structured-output contract for the Messages API.
"""
